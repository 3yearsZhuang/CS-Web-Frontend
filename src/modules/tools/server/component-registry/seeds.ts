/**
 * @file 组件注册表 — 种子数据 re-export（实际定义在 shared/db/seeds/component-seeds.ts）
 */

export type { ComponentSeed } from '@/shared/db/seeds/component-seeds';
export { COMPONENT_SEEDS } from '@/shared/db/seeds/component-seeds';
